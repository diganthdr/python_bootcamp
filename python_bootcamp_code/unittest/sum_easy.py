def easy_sum(a,b):
    if isinstance(a,str) or isinstance(a,str):
        return "Invalid inputs. Only integers supported"

    a = int(a)
    b = int(b)
    if isinstance(a,int) and isinstance(b,int):
        return a+b
    else:
        return "Invalid inputs. Only integers supported"

def convert_text_to_numbers(text):
    pass